# Thesis-Ariful

This project is part of my Master's thesis.  
Follow the steps below to correctly set up the environment and run the application.

---

## 📌 Prerequisites

Make sure you have the following installed:

- Python **3.x**
- pip
- virtualenv (comes with Python 3 by default)

Check versions:
```bash
python3 --version
pip3 --version
